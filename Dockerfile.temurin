# Dockerfile using Eclipse Temurin:21-jdk-alpine

FROM eclipse-temurin:21-jdk-alpine

WORKDIR /app

# Copy the built JAR file
COPY target/*.jar app.jar

# Expose port
EXPOSE 8080

# Command to run the application
ENTRYPOINT ["java", "-jar", "app.jar"]