# MCP Tool configuration properties

# Enable MCP tool registration (default: true)
mcp.tool.enabled=true

# MCP tool package scanning (default: com.example.notimcp.tool)
mcp.tool.packages=com.example.notimcp.tool

# MCP tool description settings
mcp.tool.description.default="Default MCP tool for notifications"